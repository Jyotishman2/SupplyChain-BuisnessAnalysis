select
    inventory_id,
    warehouse_id,
    product_id,
    snapshot_date,
    opening_stock,
    received_quantity,
    sold_quantity,
    returned_quantity,
    adjustment_quantity,
    closing_stock,
    reserved_quantity,
    sku,
    product_name,
    category_id,
    reorder_point,
    category_name,
    warehouse_name,
    warehouse_city,
    warehouse_state,
    snapshot_year,
    snapshot_month,
    available_stock,
    stock_status
from {{ source('analytics_staging', 'inventory_snapshot_enriched') }}
